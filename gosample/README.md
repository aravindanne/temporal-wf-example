to start hte temporal:
` temporal server start-dev `


to run the worker app:

` go run ./worker/ `


to run the starter app: ` go run ./starter/. `

to run the starter app with wf config: ` go run cmd/dynamic-starter/main.go configs/workflows/mytest.json `